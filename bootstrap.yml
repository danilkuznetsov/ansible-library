---
#
# Run  bootstrap playbook in new host
#
# Usage:
# ansible-playbook bootstrap.yml -v -k -i inventory/hosts
#

- hosts: all
  become: yes
  roles:
     - common
     - timezone
     - locale 
     - sysctl
     - security
     - firewall
     - swap
     - sshd
     